import React from "react";
import "./Loader.css";

const Loader: React.FC = () => {
  return <div className="loader">{/* The loader container is the one that animates */}</div>;
};

export default Loader;
